<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>PAGINA DO POST: <%= post.id %></h1>
    <p><%=post.content%></p>
    <% if (post.coments && post.coments.length > 0) { %>
        <ul>
          <% post.coments.forEach((coments) => { %>
            <li><%= coments.content %></li>
          <% }); %>
        </ul>
      <% } else { %>
        <p>Não há posts para exibir.</p>
      <% } %>
</body>
</html>